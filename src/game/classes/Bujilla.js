export class Bujilla {
    constructor(type, damage, rate, range, angle) {
        this.type = type;
        this.damage = damage;
        this.rate = rate;
        this.range = range;
        this.angle = angle;
    }
}